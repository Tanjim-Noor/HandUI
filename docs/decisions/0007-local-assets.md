# ADR 0007: Same-origin assets

**Status:** Accepted

Fetch pinned model only through explicit setup, verify checksum, and serve all runtime model/WASM files from same origin. Block third-party camera-session traffic.
