# ADR 0002: Single application

**Status:** Accepted

Keep one package with extraction-ready internal boundaries. Create publishable packages only after multiple validated consumers expose stable abstractions.
