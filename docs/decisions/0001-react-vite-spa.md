# ADR 0001: React and Vite SPA

**Status:** Accepted

Use React with strict TypeScript and Vite. HandUI is client-only and needs direct workers, WASM assets, route chunks, and static output. Next.js server features provide no current value.
